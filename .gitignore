# gh rewrites this and it can contain an auth token if the keychain is unavailable
gh/hosts.yml

# Machine-generated zsh files (ZDOTDIR points here, so zsh writes into the repo)
zsh/.zcompdump*
zsh/.zsh_history
zsh/.zprofile.zwc

# macOS
.DS_Store

