# Kitty is the terminal yazi is launched in (SUPER + F in hypr/hyprland.lua),
# so previews go through kitty's own graphics protocol rather than sixel.
[mgr]
ratio          = [1, 3, 4]
sort_by        = "natural"
sort_dir_first = true
linemode       = "size"
show_hidden    = false
show_symlink   = true

[preview]
max_width  = 1200
max_height = 1800
